# Core ML (torch/transformers/datasets обычно уже в системе; версии — минимальные)
torch>=2.1
transformers>=4.40
datasets>=2.18

# Retrieval
# <5: в 5.x загрузчик через AutoProcessor ломает офлайн-загрузку многоязычной модели
# (RU-эмбеддера). 4.x грузит через AutoTokenizer и работает офлайн.
sentence-transformers>=2.7,<5
sentencepiece>=0.2          # токенизатор многоязычных моделей (RU-эмбеддер)
faiss-cpu>=1.8
rank-bm25>=0.2.2          # stretch: hybrid retrieval

# Генеративный ридер (локальная Qwen 2.5 GGUF, офлайн)
llama-cpp-python>=0.3

# Evaluation / отчёт
evaluate>=0.4
pandas>=2.0
matplotlib>=3.7

# Ingestion
pypdf>=4.0

# Интерфейсы
streamlit>=1.33
typer>=0.12

# Тесты
pytest>=8.0
